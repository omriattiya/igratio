{    "version": "0.2.0",
    "configurations": [
        {
            "type": "node-terminal",
            "request": "launch",
            "name": "dev",
            "command": "pnpm run dev",
            "cwd": "${workspaceFolder}"
        }
    ]
}
